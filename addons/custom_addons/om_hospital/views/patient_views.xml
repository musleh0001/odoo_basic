<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Tree View-->
    <record id="hospital_patient_view_tree" model="ir.ui.view">
        <field name="name">hospital.patient.view.tree</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <tree string="Patients">
                <field name="name" />
                <field name="date_of_birth" optional="show" />
                <field name="gender" optional="hide" />
            </tree>
        </field>
    </record>

    <!-- Kanban View-->
    <record id="hospital_patient_view_kanban" model="ir.ui.view">
        <field name="name">hospital.patient.view.kanban</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_small_column">
                <field name="name" />
                <field name="date_of_birth" />
                <field name="gender" />

                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_card oe_kanban_global_click">

                            <div class="o_kanban_header">
                                <strong class="o_kanban_record_title">
                                    <field name="name" />
                                </strong>
                            </div>

                            <div class="o_kanban_content mt8">
                                <div> 🎂 DOB: <field name="date_of_birth" />
                                </div>

                                <div> ⚧ Gender: <field name="gender" />
                                </div>
                            </div>

                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <!-- Form View-->
    <record id="hospital_patient_view_form" model="ir.ui.view">
        <field name="name">hospital.patient.view.form</field>
        <field name="model">hospital.patient</field>
        <field name="arch" type="xml">
            <form string="Patient">
                <sheet>
                    <h1>
                        <field name="name" placeholder="Patient Name" />
                    </h1>

                    <group>
                        <group>
                            <field name="date_of_birth" />
                        </group>

                        <group>
                            <field name="gender" widget="radio" />
                        </group>
                    </group>
                </sheet>

                <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user" />
                    <field name="message_ids" />
                </div>
            </form>
        </field>
    </record>


    <!-- Actions -->
    <record id="hospital_patient_action" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">hospital.patient</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="view_ids"
            eval="[Command.clear(),
            (0,0,{'view_mode':'tree', 'view_id': ref('hospital_patient_view_tree')}),
            (0,0,{'view_mode':'kanban', 'view_id': ref('hospital_patient_view_kanban')}),
            (0,0,{'view_mode':'form', 'view_id': ref('hospital_patient_view_form')})]" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new patient
            </p>
        </field>
    </record>

</odoo>